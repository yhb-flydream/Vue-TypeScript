<template>
  <div></div>
</template>

<script lang="ts">
// import { Component, Prop } from 'vue-property-decorator'
// import { mixins } from 'vue-class-component' /* 引入 vue-class-component 模块，使用其自带的一些构建组件的模块 */
// import Mixin from './mixins/mixin' /* 引入声明的 mixin */

// @Component({
//   name: 'mixinIndex'
// })

/* 使用 mixins 并传入声明好的 mixin 文件，当做被 extends(继承) 的对象 */
/* Mixin 中已经 extends(继承) 了 Vue，此处就不用 extends(继承) Vue 了 */
// export default class extends mixins(Mixin) {
  // @Prop({ default: 0 }) private id!: number

  // mounted() {
  //   console.log('mixinIndex mounted')
  // }
// }
</script>
